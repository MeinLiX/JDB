@model List<string>

@{
    ViewData["Title"] = "Database";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Databases</h1>

<p>
    <a class="btn btn-success" asp-action="Create">Add New Database</a>
</p>
<table class="table table-striped">
    <thead>
        <tr>
            <th>
                Name
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (string databaseName in Model)
        {
            <tr>
                <td>
                    @databaseName <span class="badge badge-secondary text-secondary">(@ViewBag.dbLogic.GetSchemaNames(@databaseName).Count)</span>
                </td>
                <td>
                    <a class="btn btn-primary" asp-action="Redirect_Schema" asp-route-databaseName="@databaseName">Shemas</a>
                    <a class="btn btn-danger" asp-action="Delete" asp-route-id="@databaseName">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>

